import{describe,expect,it}from"vitest";import{catalog}from"../data/catalog";import{findTitle,formatRuntime,searchTitles}from"./catalog";
describe("catalog utilities",()=>{it("finds titles by slug",()=>{expect(findTitle(catalog,"film-one")?.title).toBe("Film One")});it("searches across title metadata",()=>{expect(searchTitles(catalog,"nigeria").length).toBe(3);expect(searchTitles(catalog,"comedy")[0]?.title).toBe("Film Three")});it("formats runtime consistently",()=>{expect(formatRuntime(102)).toBe("1h 42m");expect(formatRuntime(45)).toBe("45m");expect(formatRuntime()).toBe("")})});